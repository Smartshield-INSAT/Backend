import { Data } from '../../data/entity/data.entity';
import { Naming } from '../../naming/entity/naming.entity';
import { Server } from '../../server/entity/server.entity';
import { Threat } from '../../threat/threat.entity';
import { User } from '../../user/entity/user.entity';

const Entities = [Data, Server, User, Naming, Threat];

export { Entities };
